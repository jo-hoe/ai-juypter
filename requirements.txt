# Install the local package in editable mode plus dev extras
-e .[dev]

# Runtime dependencies (also declared in pyproject.toml — pip deduplicates)
anthropic>=0.40
pydantic>=2.0

# Jupyter stack
jupyterlab>=4.5.8
jupyter-ai>=3.0.0
jupyter-ai-magics>=2.31.7
langchain-anthropic>=1.4.7
ipywidgets
